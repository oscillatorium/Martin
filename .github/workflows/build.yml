name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tapok

      - name: Create mod folder
        run: |
          mkdir Martin
          cp -r content sprites mod.hjson Martin/
      
      - name: Archive mod
        run: zip -r Martin.zip Martin

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: Martin.zip